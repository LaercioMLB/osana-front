import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const H1 = styled(Box)(({ theme }) => ({
  fontSize: "21px",
  fontWeight: 600,
  color: "#757575",
  marginTop: "30px",
  marginBottom: "40px",
}));

export { H1 };
